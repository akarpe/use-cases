<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:camel="http://camel.apache.org/schema/spring"
	 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:osgi="http://www.springframework.org/schema/osgi"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://camel.apache.org/schema/spring http://camel.apache.org/schema/spring/camel-spring.xsd
       http://www.springframework.org/schema/osgi http://www.springframework.org/schema/osgi/spring-osgi.xsd">

	

	<camelContext xmlns="http://camel.apache.org/schema/spring" id="outBound"
		trace="false">
		
					<route id="outboundRoute">
				<from uri="activeMq:queue:outbound" />
				<log loggingLevel="INFO" message="${body}" />
				<camel:setHeader headerName="operationName">
					<camel:constant>executeMatchUpdate</camel:constant>
				</camel:setHeader>
				<camel:to
					uri="cxf://${webservice.url}?serviceClass={{webservice.serviceclass}}&amp;dataFormat={{dataFormat}}" />
				<camel:log message="Response is : ${body}" />
			</route>
		
	</camelContext>

</beans>
